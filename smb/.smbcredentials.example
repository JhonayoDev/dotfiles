# Plantilla de credenciales SMB
# Copiar a ~/.smbcredentials y rellenar con datos reales:
#
#   cp ~/dotfiles/smb/.smbcredentials.example ~/.smbcredentials
#   nano ~/.smbcredentials
#   chmod 600 ~/.smbcredentials
#
# NUNCA subir el archivo real al repo git.
# Solo esta plantilla está versionada.

username=
password=
domain=WORKGROUP
