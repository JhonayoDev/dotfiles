# Application
APP_NAME=myapp
SERVER_PORT=8080

# Database
DB_HOST=localhost
DB_PORT=5432
DB_NAME=mydb
DB_USER=user
DB_PASSWORD=password

# Hibernate
DDL_AUTO=update
SHOW_SQL=true
SQL_INIT_MODE=never

# Logging
LOG_LEVEL=INFO

# Docker (for docker-compose)
PROJECT_NAME=myapp
POSTGRES_DB=mydb
POSTGRES_USER=user
POSTGRES_PASSWORD=password
POSTGRES_PORT=5432
